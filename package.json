{
  "name": "jim-klonow-ski",
  "version": "2026.09.24c",
  "author": "Jim Klonowski <jim@jimklonowski.com>",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "preview": "pnpm run build && wrangler dev",
    "postinstall": "wrangler types && nuxt prepare",
    "lint": "eslint . --max-warnings 0",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:watch": "node --test --watch \"tests/**/*.test.mjs\"",
    "typecheck": "nuxt typecheck",
    "types": "wrangler types",
    "check": "pnpm run types && pnpm run lint && pnpm run typecheck && pnpm test",
    "deploy": "pnpm run check && pnpm run build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:new": "wrangler d1 migrations create DB",
    "db:status": "wrangler d1 migrations list DB --local && wrangler d1 migrations list DEMO_DB --local",
    "db:status:remote": "wrangler d1 migrations list DB --remote && wrangler d1 migrations list DEMO_DB --remote",
    "db:migrate": "wrangler d1 migrations apply DB --local && wrangler d1 migrations apply DEMO_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote && wrangler d1 migrations apply DEMO_DB --remote",
    "sync:local": "node scripts/sync-local.mjs",
    "sync:local:r2": "node scripts/sync-r2-local.mjs",
    "demo:generate": "node scripts/demo/generate-demo-data.mjs",
    "demo:seed:local": "node scripts/demo/seed-demo.mjs --local",
    "demo:seed:remote": "node scripts/demo/seed-demo.mjs --remote"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@nuxt/ui": "^4.11.2",
    "@nuxtjs/seo": "5.3.16",
    "@unhead/vue": "^3.4.1",
    "@vueuse/core": "^14.4.0",
    "@vueuse/nuxt": "^14.4.0",
    "exifr": "^7.1.3",
    "nuxt-security": "^2.6.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@comark/nuxt": "0.7.0",
    "@iconify-json/lucide": "^1.2.136",
    "@iconify-json/simple-icons": "^1.2.97",
    "@nuxt/eslint": "^1.17.0",
    "@types/node": "^26.6.2",
    "echarts": "^6.1.0",
    "eslint": "^10.11.0",
    "nitro-cloudflare-dev": "^0.2.2",
    "nuxt": "4.5.2",
    "nuxt-echarts": "^1.1.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^6.0.3",
    "vue": "catalog:",
    "vue-echarts": "^8.3.0",
    "vue-tsc": "^3.3.11",
    "wrangler": "^4.137.0"
  },
  "packageManager": "pnpm@11.7.0"
}